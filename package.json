{
    "name": "kitsune-e2e",
    "private": true,
    "description": "Browser layer only. Node is confined to this and never reaches the default Pest suite (ADR-024).",
    "scripts": {
        "test:e2e": "playwright test",
        "test:e2e:ui": "playwright test --ui"
    },
    "devDependencies": {
        "@playwright/test": "^1.56.0"
    },
    "dependencies": {
        "@axe-core/playwright": "^4.13.0"
    }
}
